# Конфиг для автобиддера WB

CONTROLLED_ITEMS = [
    {
        "advert_id": 29731520,      # ID рекламной кампании WB
        "nm_id": 265104282,        # НМ товара
        "placement": "search",     # search или recommendations

        "campaign_type": "search",  # тип кампании: search / auto (автореклама, полки)

        # Коридор ставок
        "min_bid": 400,
        "max_bid": 2000,

        # Стартовая ставка
        "base_bid": 580,

        # Текущее "целевое" значение, от него сейчас работает бидер
        # (оставляем, чтобы старый код не упал)
        "target_bid": 580,

        # Целевой CPO (стоимость заказа) в рублях для ЭТОЙ кампании
        "target_cpo": 160,

        # Минимум данных для принятия решения
        "min_clicks": 20,      # не трогаем ставку, пока меньше кликов
        "min_orders": 2,       # и меньше заказов

        # Насколько резко двигаем ставку (в процентах)
        "step_up_pct": 0.15,   # +15%, если CPO лучше цели
        "step_down_pct": 0.15, # -15%, если CPO хуже цели

        # За сколько дней смотреть статистику
        "lookback_days": 3,
    },
]

BIDDING_RULES = {
    "min_clicks": 20,
    "min_orders": 2,
    "step_up_pct": 0.15,
    "step_down_pct": 0.15,
    "cpo_tolerance_pct": 0.20,
}

CHECK_INTERVAL_SEC = 300  # раз в 5 минут проход
TIME_SHIFT_HOURS = 3  # на сколько часов локальное время опережает время сервера

# Включение новой CPO-логики (через compute_new_bid_cpo_v2).
# По умолчанию выключено, чтобы поведение бота НЕ менялось.
USE_CPO_V2: bool = False
